{
  "name": "tmux-web",
  "version": "0.3.2",
  "description": "Access tmux sessions from your browser via a web-based terminal",
  "type": "module",
  "bin": {
    "tmux-web": "dist/index.js"
  },
  "files": [
    "dist",
    "scripts/fix-pty-perms.mjs"
  ],
  "scripts": {
    "postinstall": "node scripts/fix-pty-perms.mjs",
    "prepublishOnly": "tsc && bun run build:client && bun run build:exts",
    "build": "tsc && bun run check:drawer-scripts && bun run build:client && bun run build:exts",
    "check:drawer-scripts": "tsx scripts/check-drawer-scripts.ts",
    "build:client": "bun scripts/build-client.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "coverage": "vitest run --coverage",
    "build:exts": "node scripts/build-exts.mjs --force",
    "start": "node dist/index.js",
    "predev": "bun scripts/build-client.mjs && node scripts/build-exts.mjs",
    "dev": "TMUX_WEB_DEV=1 tsx src/index.ts"
  },
  "keywords": [
    "tmux",
    "terminal",
    "web",
    "pty",
    "websocket"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ashutoshpw/tmux-web"
  },
  "packageManager": "bun@1.4.2",
  "engines": {
    "node": ">=22",
    "bun": ">=1.4.2"
  },
  "trustedDependencies": [
    "node-pty"
  ],
  "dependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "@hono/node-server": "^1.19.12",
    "hono": "^4.7.0",
    "lowdb": "^7.0.1",
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^25.8.0",
    "@types/ws": "^8.5.0",
    "@vitest/coverage-v8": "^2.1.9",
    "tsx": "^4.21.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.9"
  }
}
